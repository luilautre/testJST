{
  "builds": [{ 
    "src": "server.js", 
    "use": "@vercel/node",
    "config": {
      "includeFiles": ["variables.json", "public/**"]
    }
  }],
  "routes": [{ "src": "/(.*)", "dest": "server.js" }]
}
