{
  "builds": [{ 
    "src": "server.js", 
    "use": "@vercel/node",
    "config": {
      "includeFiles": ["variables.json", "functions.js", "public/**"]
    }
  }],
  "routes": [{ "src": "/(.*)", "dest": "server.js" }]
}
